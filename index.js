import { mostrarProductos } from "./views/cargarProductos.js";

// Selecciona el contenedor del DOM
let contenedor = document.querySelector("#root");

// Llama a la función y pasa el contenedor
mostrarProductos(contenedor);